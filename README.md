# WeekCrew

WeekCrew — веб-приложение-PWA для «кружков недели». Каждые 7 дней мы собираем маленькие ламповые команды по интересам (3–8 человек), даём айсбрейкеры и тёплый таймер общения, а затем мягко предлагаем новый круг.

## Основные фичи

- 🎯 **Выбор интереса** и автоматический подбор активного кружка либо создание нового.
- 🧊 **Айсбрейкер дня** и простая лента сообщений без лайков и токсичных рейтингов.
- 🔐 **Анонимная идентификация устройства** (deviceId + опциональная Firebase Anonymous Auth).
- 🔄 **Автоперемешивание людей каждую неделю**: по завершении кружок уходит в read-only.
- 📱 **PWA / mobile-first**: manifest, сервис-воркер, плавные анимации через Framer Motion.

## Технологический стек

- [Next.js 14 (App Router)](https://nextjs.org/)
- [TypeScript](https://www.typescriptlang.org/)
- [Tailwind CSS](https://tailwindcss.com/)
- [Framer Motion](https://www.framer.com/motion/)
- [Zustand](https://github.com/pmndrs/zustand) для состояния устройства/пользователя/кружка
- [Firebase Firestore](https://firebase.google.com/products/firestore) как БД + анонимная Auth

## Требования

- Node.js 18+
- npm 9+ (или pnpm/yarn по желанию)
- Учётка в [Firebase Console](https://console.firebase.google.com/) (Spark план достаточно)

## Быстрый старт

```bash
# 1. Установка зависимостей
npm install

# 2. Копируем переменные окружения
cp .env.example .env.local
# затем заполните .env.local значениями из Firebase → Project settings → General

# 3. Запуск dev-сервера
npm run dev

# 4. Открываем http://localhost:3000
```

### Сборка и прод-режим

```bash
npm run build
npm start
```

### Линтер и форматирование

```bash
npm run lint
npm run format
```

## Переменные окружения

| Название | Описание |
| --- | --- |
| `NEXT_PUBLIC_FIREBASE_API_KEY` | API ключ из Firebase Project settings |
| `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN` | Auth domain вида `your-app.firebaseapp.com` |
| `NEXT_PUBLIC_FIREBASE_PROJECT_ID` | Идентификатор проекта |
| `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET` | Storage bucket (например, `your-app.appspot.com`) |
| `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID` | Messaging sender ID |
| `NEXT_PUBLIC_FIREBASE_APP_ID` | App ID из настроек Firebase |
| `NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID` | (опционально) Google Analytics |

> Все переменные читаются только на клиенте (префикс `NEXT_PUBLIC_`). Без заполненного `.env.local` приложение работает в offline-демо режиме (сообщения не сохранятся).

## Настройка Firebase

1. Создайте проект в Firebase Console.
2. Добавьте Web App (`</>`) и выпишите config (API key и т.д.).
3. Включите **Firestore** (режим production с необходимыми правилами безопасности).
4. Включите **Anonymous Authentication** в разделе Auth → Sign-in method.
5. Задайте коллекции:
   - `circles`
   - `messages`
   - (опционально) `profiles` — для будущих расширений.
6. Внесите конфиг в `.env.local`.

## Структура проекта

```
src/
  app/
    (routes)/circle
    (routes)/explore
    (routes)/settings
    layout.tsx, page.tsx, globals.css
  components/
    circle/… (лента, айсбрейкеры, композер)
    layout/… (header, footer)
    providers/ClientProviders (deviceId + сервис-воркер + тема)
    shared/…
  config/firebase.ts (инициализация SDK)
  constants/, data/, hooks/, lib/, store/, types/
public/
  manifest.json, icon.svg, service-worker.js
```

## Сервис-воркер и PWA

- `public/manifest.json` описывает приложение и иконку (`public/icon.svg`).
- `public/service-worker.js` кеширует базовые ассеты и включает оффлайн-фолбэк на главную страницу.
- Регистрация сервис-воркера происходит в `ClientProviders` после гидратации.

## Состояние и хранилище

- **Zustand** хранит `device`, `user`, `settings`, `circle` и `messages`.
- Состояние (`device`, `user`, `settings`) персистится в `localStorage`.
- `deviceId` создаётся один раз и доступен через `src/lib/device.ts`.

## Участие в кружке

- `/explore` — выбор интересов, поиск активного кружка и создание нового при необходимости.
- `/circle` — отображение текущего кружка, ленты сообщений и айсбрейкера дня.
- `/settings` — управление темой, языком, анимациями и кнопка сброса локальных данных.

### Логика живого кружка

- При подборе сохраняем кружок в Firestore: `circles` содержит интерес, capacity, статус, `memberIds`, `createdAt` и `expiresAt` (7 дней вперёд).
- `messages` хранит обсуждения (`circleId`, `authorDeviceId`, текст, `createdAt`). На клиенте всё синхронизируется в реальном времени.
- Кружок доступен для ответа ровно 7 дней: после `expiresAt` или статуса `archived` страница переходит в read-only и предлагает найти новый круг.
- Для уюта действует мягкий лимит — не больше 7 сообщений в сутки на устройство. При достижении лимита поле ввода отключается до следующего дня.

## Деплой

### Vercel

1. `vercel init` или импорт репозитория в Vercel Dashboard.
2. Укажите все переменные `.env.local` в настройках проекта (Environment Variables).
3. Деплой осуществляется автоматически через `git push`.

### Альтернативы

- **Render / Railway / Netlify** — запустить `npm run build` и `npm start`.
- **Self-hosted** — любой Node.js сервер с поддержкой Next.js 14.

## Команды npm

| Скрипт | Назначение |
| --- | --- |
| `npm run dev` | Запуск dev-сервера на `http://localhost:3000` |
| `npm run build` | Сборка Next.js в прод-режиме |
| `npm start` | Запуск production-сервера |
| `npm run lint` | Проверка ESLint |
| `npm run format` | Форматирование кода Prettier |

## План разработки

Подробный поэтапный план см. в [PLAN.md](./PLAN.md).

---

Если появятся вопросы или идеи — пиши на `hey@weekcrew.app`. Добро пожаловать в кружок недели! 💜
